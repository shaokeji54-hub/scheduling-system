# 协作纠错复盘 - V1.0.0 to V1.1.0

回顾本次协作中被纠正过的所有内容，逐条分析根因，并给出下次开头的指令建议。

| 修改内容 | 错误归因 | 下次指令建议 |
|---------|---------|------------|
| 排班引擎 time(hour=24) 崩溃 | 判断逻辑问题：未验证 min(...,24) 产生的值在 time() 合法范围(0-23) | 新增代码前先列出所有可能越界的数值范围并添加 guard clause |
| CoverageGap 缺 position_name 导致 500 | 判断逻辑问题：引擎返回的 gap dict 不含 position_name，但 Pydantic Schema 要求该字段 | 对照 Pydantic schema 检查所有构造点；首次运行后用 API 测试验证 |
| 重新生成已有周报错(唯一约束) | 判断逻辑问题：未考虑重复生成场景 | 为所有 POST 创建类接口预判重复请求场景(幂等/覆盖/冲突) |
| str.replace 目标字符串不匹配 | 信息不足：replace 目标 import csv,io,os 与实际文件 import csv,io 不匹配 | 文件替换后用 diff 验证；涉及 import 用 apply_patch |
| scalar_one_or_none 忘记加 () | 判断逻辑问题：方法对象赋值给变量，if 永远为 True | 用注释标注：.scalar_one_or_none()  # -> 返回行或None |
| 前端 Tab 切换不加载 detail 数据 | 判断逻辑问题：Tabs onChange 只处理了 hourly 和 monthly，遗漏 detail | Tabs onChange 写成 switch 语句，每个 case 显式标注 |
| 排班面板只显示有排班的员工 | 判断逻辑问题：Object.values(empMap) 只遍历了有 assignment 的员工 | 从全员列表生成行，无班次者显示休 |
| apply_patch 因 BOM 匹配失败 | 信息不足：未意识到文件以 BOM 开头 | 文件操作前先检查和清理 BOM |
| 时间段弹窗只编辑未设岗位 | 判断逻辑问题：设计上只允许新增，不允许修改已有 | 全部可编辑模式，表格内直接下拉选择 |
| seed.py 将 Position 赋值给 Skill | 判断逻辑问题：emp.skills 是 Skill 关系但传入了 Position 对象 | 种子脚本中每个关系赋值前打印 type() 确认目标类型 |
