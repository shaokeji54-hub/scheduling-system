# 本次协作纠错反思表

> 回顾本次 V1.1.0 开发中被用户纠正过的所有问题。

| # | 修改内容 | 错误归因 | 下次指令建议 |
|---|----------|----------|-------------|
| 1 | **人力计算: 产能×人力 >= 业务量 且 人力为整数** | 判断逻辑问题。用了 `round(required, 1)` 而非 `math.ceil()`, 导致可能产能不足; 且返回浮点数而非整数。 | 开头增加: "所有涉及人力/人数/headcount的计算结果必须用 `math.ceil` 向上取整, 确保产能×人力 >= 业务量" |
| 2 | **休假管理增加管理员代填 + 操作标注** | 信息不足。只实现了员工自填, 未考虑排班员有代填需求。 | 开头增加: "休假管理需要排班员和员工两个入口, 排班员操作需写入 AdjustmentLog 审计" |
| 3 | **月度汇总表格改为7天横轴导航** | 信息不足。默认展示全部日期, 用户要每周滚动查看。 | 开头增加: "日期范围的数据表需设计为按周分页, 左右箭头导航, 每周固定7列" |
| 4 | **按小时视图: 0:00-24:00 每半小时一格, 行列互换** | 信息不足+判断逻辑。最初设计是小时×岗位, 用户要岗位×半小时时间网格。 | 开头增加: "时间轴类视图默认从0:00到24:00, 半小时粒度; 先问用户要岗位×时间还是时间×岗位" |
| 5 | **岗位时间段设置: 所有岗位同时可编辑 + Gantt 图表** | 信息不足。开始时只能逐个编辑, 需要所有岗位列在一起一次性改完。 | 开头增加: "配置表格设计为全部行同时可编辑模式, 带实时图表预览" |
| 6 | **每人每天班次为完整时间段不拆分** | 判断逻辑问题。引擎可能为同一人分配多个不连续的班次片段。 | 开头增加: "排班引擎确保每人每天只有一个连续班次, 覆盖其岗位时间段" |
| 7 | **排班面板显示所有员工(含无排班) + 多岗位** | 信息不足。只显示有排班数据的员工, 用户要看到全员。 | 开头增加: "透视表须包含所有员工(包括无排班的为休), 不同岗位不同颜色" |
| 8 | **警告标记在单元格内显示 + 表格上方列出详情** | 信息不足。警告只给了总数, 用户要看到具体每条详情。 | 开头增加: "警告/缺口信息须同时展示: (1)单元格内标记; (2)表格上方列出每条详情" |
| 9 | **明细列表时段显示范围而非单点** | 判断逻辑问题。显示 `h:00` 而不是 `h:00-(h+1):00`。 | 开头增加: "时间段显示必须包含起止范围, 如 9:00-10:00" |
| 10 | **排班引擎 time(hour=24) 崩溃修复** | 判断逻辑问题。`min(x, 24)` 生成 24, Python time() 只接受 0-23。 | 开头增加: "所有 time() 构造的 hour 参数范围必须是 0-23" |
| 11 | **CoverageGap position_name 缺失导致500** | 判断逻辑问题。新加 schema 字段后未同步更新数据生产者(引擎)。 | 开头增加: "新增或修改 Pydantic schema 字段后, 检查所有数据来源是否都能提供该字段" |
| 12 | **PowerShell 编码问题导致 Python `-c` 命令失败** | 环境限制。Win 终端 GBK 编码与内含中文的字符串冲突。 | 开头增加: "复杂 Python 代码(含中文/多行/嵌套引号)写到临时 .py 文件执行, 不用 `-c` 参数" |
